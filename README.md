# Многомерный статистический анализ социально-экономических показателей регионов

**Цель исследования:** с помощью методов снижения размерности, кластеризации и регрессии выявить основные факторы, влияющие на целевой показатель регионов России, и сгруппировать регионы по схожим характеристикам.

---

## Данные

| Код | Показатель |
|-----|------------|
| **X1** | Численность населения с доходами ниже прожиточного минимума (%) |
| **X2** | Среднедушевые доходы населения (в месяц), руб. |
| **X3** | Потребительские расходы в среднем на душу населения (в месяц), руб. |
| **X4** | Смертность населения трудоспособного возраста (на 100 000 человек) |
| **X5** | Заболеваемость на 1000 человек населения |
| **X6** | Численность населения на одну больничную койку |
| **X7** | Выбросы загрязняющих веществ в атмосферный воздух (на 10 000 человек, тыс. тонн) |
| **X8** | Использование свежей воды (на 10 000 человек, млн м³) |
| **X9** | Сброс загрязненных сточных вод (на 10 000 человек, млн м³) |

---

## Структура репозитория
<pre>
README.md                                     # Описание проекта
multivariate_analysis_of_economic_indicator/
├── data/                                                
│   └── data.xlsx                             # Исходные данные
├── notebooks/                                                            
│   └── multivariate_analysis.ipynb           # Jupyter Notebook с полным анализом
└── images/                                   # Результаты кластеризации, визуализации PCA                  
</pre>

---

## Методы анализа

### 1. Факторный анализ (PCA)
  #### 1.1. Проверка значимости корреляционной матрицы 
**Тест Бартлетта** на сферичность: χ² = 324.51, p < 0.01. [`Корреляционная матрица`](multivariate_analysis/images/correlation%20matrix.png) значима → применение PCA обосновано.

#### 1.2. Собственные значения и доля объяснённой дисперсии
| Компонента | Собственное значение (λ) | % дисперсии | Накопленный % |
|------------|--------------------------|-------------|---------------|
| **PC1**    | 3.279                    | 36.44%      | 36.44%        |
| **PC2**    | 1.702                    | 18.92%      | 55.36%        |
| **PC3**    | 1.083                    | 12.03%      | **67.39%**    |

**Критерий Кайзера** (λ > 1): оставляем **[`3 главные компоненты`](multivariate_analysis/images/eigenvalue%20of%20correlation%20matrix.png)** , объясняющие **67.4%** общей дисперсии.

#### 1.3. Матрица нагрузок 
```python
num_components = 3  # задаем нужное количество компонент
sqrt_l = np.eye(num_components)
for i in range(num_components):
    sqrt_l[i][i] = math.sqrt(eigvals_sorted[i])
u_array = u.values if hasattr(u, 'values') else u
A = np.dot(u_array[:, :num_components], sqrt_l)
A_df = pd.DataFrame(
    data=A,
    columns=['F' + str(i+1) for i in range(num_components)],
    index=[f'X{i+1}' for i in range(9)]
)
print(f"Матрица нагрузок для {num_components} главных компонент:")
print(A_df.round(4))
```
В результате чего **матрица нагрузок** имеет вид:
```math
\begin{pmatrix}
-0.68 & -0.30 & -0.38 \\
0.83 & 0.37 & -0.12 \\
0.73 & 0.43 & 0.35 \\
0.48 & -0.75 & -0.12 \\
0.67 & -0.20 & -0.42 \\
-0.47 & 0.72 & -0.14 \\
0.61 & 0.25 & -0.64 \\
0.32 & 0.02 & 0.15 \\
0.48 & -0.33 & 0.40
\end{pmatrix}
```
**столбцы соответствуют F1, F2, F3, а строки - переменным Х1-Х9*

**Интерпретация главных компонент** следующая:
| Компонента |  Нагрузки <br> (r > 0.55)          | Экономический смысл |
|------------|-------------------------------|---------------------|
| **F1** |(-): Х1 <br> (+): Х2, X3, X5, X7 | Показатель социально-экономического благополучия|
| **F2** |(+):X4, X6 | Показатель системы здравоохранения|
| **F3** |(-):X7 | Показатель загрязнения воздуха |

---
### 2. Кластерный анализ
- Применены **иерархическая кластеризация (метод Уорда)** и **K‑средних**.

#### 2.1. Метод Уорда (иерархическая кластеризация)
```python
def ward_clustering(n_clusters):
    clusters_ward = fcluster(Z, n_clusters, criterion='maxclust')
    result[f'Кластер_Уорд_{n_clusters}'] = clusters_ward
    for cluster_num in range(1, n_clusters + 1):
        cluster_regions = result[result[f'Кластер_Уорд_{n_clusters}'] == cluster_num].index.tolist()
        print(f"\nКластер {cluster_num} ({len(cluster_regions)} регионов):")
        print(", ".join(cluster_regions))
    return clusters_ward
n_clusters_list = [7]
for n_clusters in n_clusters_list:
    ward_clustering(n_clusters)
```
По [`методу Уорда`](multivariate_analysis/images/Ward%20PCA.png) при пороговом значении ρ = 10.45 все регионы РФ разбиваются на **5 кластеров**. 

#### 2.2. Метод K-средних
```python
from sklearn.cluster import KMeans
def elbow_method(data, max_k=10):
    wcss = []  # Within-Cluster Sum of Square
    for k in range(1, max_k + 1):
        kmeans = KMeans(n_clusters=k, random_state=42, n_init=10)
        kmeans.fit(data)
        wcss.append(kmeans.inertia_)
    return wcss
max_k = min(10, len(result) - 1)
wcss = elbow_method(result, max_k)
kmeans_7 = KMeans(n_clusters=7, random_state=42, n_init=10)
kmeans_labels_7 = kmeans_7.fit_predict(result)
result['Кластер_Kmeans_7'] = kmeans_labels_7 + 1  # +1 чтобы кластеры начинались с 1
for cluster_num in range(1, 8):
    cluster_regions = result[result['Кластер_Kmeans_7'] == cluster_num].index.tolist()
    print(f"\nКластер {cluster_num} ({len(cluster_regions)} регионов):")
    print(", ".join(cluster_regions))
```
[`Методом К-средних`](multivariate_analysis/images/K-means%20PCA.png)** все субъекты РФ разбиваются на **7 кластеров**.

### 3. Регрессионный анализ (OLS)

#### 3.1. Оценка коэффициентов уравнения регрессии
| Переменная | β* (стандартизованный) | Std.Err. β* | b (нестандартизованный) | Std.Err. b | t(81) | p-value |
|------------|------------------------|-------------|-------------------------|------------|-------|---------|
| **Intercept** | — | — | 66.7878 | 0.1778 | 375.6772 | 0.000000 |
| **Factor 1** | 0.3808 | 0.0514 | 1.3256 | 0.1788 | 7.4125 | 0.000000 |
| **Factor 2** | -0.7789 | 0.0514 | -2.7111 | 0.1788 | -15.1600 | 0.000000 |
| **Factor 3** | 0.1857 | 0.0514 | 0.6465 | 0.1788 | 3.6150 | 0.000520 |

#### 3.3. Уравнение регрессии
Исходя из полученных данных, оценка уравнения* регрессии выглядит следующим образом:
```math
$$
\begin{aligned}
\hat{y} &= 66.7878 + 1.3256 \cdot f_1 - 2.7111 \cdot f_2 + 0.6465 \cdot f_3 \\
        &\quad\;\, (0.1778) \quad\;\; (0.1788) \quad\;\; (0.1788) \quad\;\; (0.1788)
\end{aligned}
$$
```
**где в скобках указаны стандартные ошибки коэффициентов.*

**Коэффициент детерминации** составил R² = 0.7862. Это значит, что доля вариации ожидаемой продолжительности жизни, обусловленная тремя главными компонентами, составляет 78.62%, то есть на 21.38% зависит от неучтенных в модели факторов.

**Результаты** построения модели при использовании регрессионного анализа:
| Показатель | Значение |
|------------|----------|
| **R²** | 0.786 |
| **Скорректированный R²** | 0.778 |
| **F-статистика** | 99.28 |
| **p-value** | < 0.001 |
  
---

## Результаты
- **PCA** позволил сократить размерность с 8 до 3 факторов без существенной потери информации.
- **Кластеризация** выявила четкие группы регионов, что может быть использовано для региональной политики.
- **Регрессионная модель** показала, что основными драйверами целевого показателя являются факторы X3, X5 и X8, а фактор X2 действует угнетающе.
